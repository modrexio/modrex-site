Hairline progress indicator for installs and app updates.

```jsx
<ProgressBar percent={62} />
<ProgressBar />            {/* indeterminate pulse */}
```

Sits flush at the bottom of a card or top bar.
