The base raised surface for grouping content (settings sections, mod cards, list rows).

```jsx
<Card>
  <h2 style={{margin:0, fontSize:14, fontWeight:600}}>Game Path</h2>
  <p style={{color:'var(--text-tertiary)', fontSize:12}}>Path to your PAYDAY 3 install.</p>
</Card>

<Card padding={false}>{/* image-led mod card */}</Card>
```

Border + raised surface only — no shadow.
